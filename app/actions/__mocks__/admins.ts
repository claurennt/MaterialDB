export const registerAdmin = jest.fn().mockResolvedValue({ success: true });
export const activateAccount = jest.fn().mockResolvedValue({ success: true });
export const modifyPassword = jest.fn().mockResolvedValue({ success: true });
export const deleteOwnProfile = jest.fn().mockResolvedValue({ success: true });
