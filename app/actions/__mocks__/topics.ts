export const createTopic = jest.fn().mockResolvedValue({ success: true });
export const updateTopic = jest.fn().mockResolvedValue({ success: true });
export const deleteTopic = jest.fn().mockResolvedValue({ success: true });
